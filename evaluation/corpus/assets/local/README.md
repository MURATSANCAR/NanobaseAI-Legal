# Local corpus assets

Place licensed evaluation binaries here (or set `NANOBASE_CORPUS_ASSET_ROOT`).

Do **not** commit PDFs/DOCX/images. Use `scripts/corpus_intake.py` for read-only intake.

Suggested layout under the asset root:

```text
native/
scanned/
docx/
table/
knowledge/
```
