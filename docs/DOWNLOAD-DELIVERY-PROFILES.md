# Download Delivery Profiles

`ObjectDeliveryStrategy` / `ProfileAwareObjectDeliveryStrategy`.

Fallback telemetry codes: `DELIVERY_PROXY_ONLY`, `PRESIGN_PUBLIC_ENDPOINT_INVALID`.

Proxy path remains production-validated.
