# DOCX Table Support

DOCX tables map into the canonical `document_table` / `document_table_cell` model with merged-cell spans and header-row context.

Nested tables and cell paragraphs retain `source_xml_path` when DOCX structure pipeline is enabled.

Live E2E: PENDING.
