# Requirement Performance Profiling

`requirement_extraction_timing` stores stage durations (queue/capacity/model/schema/grounding/persist) without raw text.

Budgets: `performance_budget_profile` slices for native/scanned/DOCX/table/long docs.

Flag: `REQUIREMENT_EXTRACTION_TIMING_ENABLED`.
