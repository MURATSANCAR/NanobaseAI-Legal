# Certificate Extraction

Required fields when present: issuer, certificate number, standard, scope, issue date, expiry date, company name, status.

Missing expiry must remain `not found` / validity `UNKNOWN` — never invented.

Validity: `DeterministicKnowledgeValidityEvaluator`.
