# Report Visual Validation

`ReportVisualStructureValidator` checks PDF magic, page markers, placeholder/cut-text signals, missing required section tokens, replacement glyphs.

Persisted in `report_visual_validation_result` when `REPORT_VISUAL_VALIDATION_ENABLED`.
