# Corpus Management

Tables: `evaluation_corpus`, `corpus_document`, `corpus_annotation` (V33).

Rules: no real customer secrets; license/provenance required; production logs must not dump corpus binaries.

Harness: `scripts/corpus_e2e_harness.py`.
